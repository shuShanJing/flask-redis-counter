services:
  web:
    build: .
    ports:
      - "5000:5000"
    depends_on:
      - redis
  redis:
    image: "redis:alpine"
    restart: always
    volumes:
      - redis_data:/data
volumes:
  redis_data:
